import { SxProps, Theme } from '@mui/material';

export const homePageStyles = {
    container: {
        display: 'flex',
        flexDirection: 'column',
        gap: 4,
        width: '100%',
    } as SxProps<Theme>,
};
